= form_for @school, html: {class: 'form-inline', role: 'form'}  do |f| 
  = render 'shared/error_messages', :target => @school
  .form-group
    = f.text_field :name, class: 'form-control', placeholder: 'School name'
    = f.submit 'Create', class: 'btn btn-default'
